# Docker 镜像源配置
# 如果默认镜像源访问失败，取消下面的注释并选择一个可用的镜像源

# 可选镜像源（选择一个可用的）：
# DOCKER_REGISTRY=docker.m.daocloud.io
# DOCKER_REGISTRY=docker.1panel.live
# DOCKER_REGISTRY=hub.rat.dev
# DOCKER_REGISTRY=dockerproxy.com

# 官方镜像（默认）
DOCKER_REGISTRY=docker.io
